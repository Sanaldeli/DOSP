@model DOSP.Models.Category

@{
    ViewBag.Title = "Kategori Ekle/Güncelle";
    ViewBag.nav = "aPanel";
}

@section nav {
    <a href="/AdminPaneli">Admin Paneli</a>
    <a href="/Kategori">Kategoriler</a>
    <a>@ViewBag.Title</a>
}

<h2>@ViewBag.Title</h2>

<form action="/Kategori/Ekle" method="post" role="form" class="form-col">
    @if (Model != null)
    {
        <input type="hidden" name="ID" value="@Model.ID" />
    }
    @if (Model.Name == null)
    {
        <input style="padding: 5px;" value="" type="text" name="Name" placeholder="Kategori adını giriniz" required />
    }
    else
    {
        <input style="padding: 5px;" value="@Model.Name" type="text" name="Name" placeholder="Kategori adını giriniz" required />
    }
    <button type="submit" class="btn btn-yellow">Kaydet</button>
</form>

<div style="font-size: 12px; line-height: 15px;">
    <a href="/Kategori">< Listeye Dön</a>
</div>